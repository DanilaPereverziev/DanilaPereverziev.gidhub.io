let speed = 10,
    html = (500,500)
    x = 50,
    y = 10,
    player = document.querySelector(".player");
    if(keypress === 'Z'){
        y - speed;
    }
    draw.rect(".player", (0,255,255), (x, y))